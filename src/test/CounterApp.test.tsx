import { render, screen } from "@testing-library/react";
import CounterApp from "../CounterApp";

describe("App", () => {
  
  it("should work as expected", () => {
    // render(<CounterApp(objecto) />);
    expect(1 + 1).toBe(2);
  });
});